NEXT_PUBLIC_SANITY_PROJECT_ID="odswazx4"
NEXT_PUBLIC_SANITY_DATASET="production"
NEXT_PUBLIC_SANITY_API_VERSION="2026-05-01"

# Merch checkout (Stripe). The secret key is server-only — set the real value
# in .env.local and Vercel, never commit it, never prefix it with NEXT_PUBLIC.
STRIPE_SECRET_KEY=""
# Optional fallback for Stripe redirect URLs, e.g. "https://whalefall-site.vercel.app"
NEXT_PUBLIC_SITE_URL=""
